/** Layout metrics for Twitch clip cards painted inside the chat ring. */

/** Pixel height reserved under a message for one clip card. */
export const CLIP_CARD_HEIGHT_PX = 76;
/** Thumbnail column width (matches prior HTML card). */
export const CLIP_CARD_THUMB_W = 118;
/** Gap between message body and card top. */
export const CLIP_CARD_GAP_PX = 4;
/** Max card width inside the chat column. */
export const CLIP_CARD_MAX_W = 420;
export const CLIP_CARD_RADIUS = 10;
export const CLIP_CARD_META_GAP = 10;
export const CLIP_CARD_TITLE_COLOR = 0xc4b5fd;
export const CLIP_CARD_BG = 0x1a1a22;
export const CLIP_CARD_BORDER = 0xffffff;
